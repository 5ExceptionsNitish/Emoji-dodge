# Prompt history

User prompts from this project chat (chronological).

1. **Camera crash (`CameraFaceView.swift`)**  
   > App is crashing. Verify and fix that into cameraFaceView.swift file  
   > `*** Terminating app due to uncaught exception 'NSInvalidArgumentException', reason: '*** -[AVCaptureConnection setVideoMirrored:] Cannot be set when automaticallyAdjustsVideoMirroring is YES'`

2. **Gameplay: lanes + manual player**  
   > I have to change into the game it not have the user from the camera it have manual player at the bottom and user can move the character with the drag gesture and something. So it should have 5 lines to manage the player into saving the vertical drop of emojis.

3. **Background music**  
   > I have the sample music added into the workspace which should play at the starting and we can mute the song from home screen but for now music is not working.

4. **Use `theme_song.mp3`**  
   > theme_song.mp3 stored but you did not used it for now.

5. **Home screen polish**  
   > Fine tune the first home screen properly.

6. **Score vs emoji drops**  
   > Also the emoji drops and the counter I think is little bit mismatch so verify it properly for verification.

7. **Home screen header controls**  
   > the volume and menu button is not proper on the homescreen.

8. **Lane movement (no skipping)**  
   > user should move on single lane side by side but for now it is skipping the lanes

9. **This prompt file**  
   > Create a prompt.md file all the promts that i give you

---

*Generated to preserve prompt history for the EmojiFaceDodge project.*
